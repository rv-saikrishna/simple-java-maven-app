version: 2.1

orbs:
  maven: circleci/maven@1.0.2

jobs:
  build:
    docker:
      - image: cimg/openjdk:8.0
    steps:
      - checkout
      - maven/with_cache:
          steps:
            - run: bash jenkins/scripts/deliver.sh
      - persist_to_workspace:
          root: .
          paths:
            - ./

  publish-github-release:
    docker:
      - image: circleci/golang:1.8
    steps:
      - attach_workspace:
          at: ./artifacts
      - add_ssh_keys:
          fingerprints:
            - "70:8a:91:af:e3:3e:25:de:09:eb:4b:ad:ab:cd:99:3a"
      - run:
          name: "Publish Release on GitHub"
          command: |
            go get github.com//github-release/github-release
            cd artifacts &&  VERSION=$(cat pom.xml | grep "<version>.*</version>" | head -1 |awk -F'[><]' '{print $3}')
            git tag $VERSION && git push --tags
            github-release upload \
            --user ${CIRCLE_PROJECT_USERNAME} \
            --repo ${CIRCLE_PROJECT_REPONAME} \
            --tag ${VERSION} \
            --name "simple-app-jar" \
            --file artifacts/target/*.jar

workflows:
  build-and-deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
                - master
            tags:
              ignore:
                - /v.*/
      - publish-github-release:
          filters:
            branches:
              only:
                - master
          requires:
            - build
